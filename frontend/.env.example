# Frontend API base URL.
# 开发：复制本文件为 .env.local 即可（默认指向本机后端）。
# 生产：留空 → 走相对路径，由 Nginx 同源反代到后端（见已提交的 .env.production）。
#       切勿在生产构建里填 127.0.0.1:8000，否则浏览器会请求访客本机、被 CORS 拦截。
VITE_API_BASE=http://127.0.0.1:8000
